kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30081   # NodePort del servicio de Matomo
        hostPort: 8081         # Puerto en tu máquina
        protocol: TCP
